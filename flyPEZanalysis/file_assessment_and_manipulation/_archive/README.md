# file_assessment_and_manipulation — archive

Ad-hoc data repair and auditing scripts: `directoryAssessment`, `runStatsFixer`,
`pez3000_exptID_correction`, `pez3000_variableAssessment`, `findEdgeGaps_tracking`,
`rawDataAnalysis_exploration`, and the `getAllFiles` helper.

The three live files stay in the parent folder: `pez3000_statusAssessment_v2.m`,
`pez3000_posthoc_corrections.m` (both run nightly), and `fixManAnno.m`.
